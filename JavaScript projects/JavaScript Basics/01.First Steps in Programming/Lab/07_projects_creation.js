function projectsCreation(architectName, countProjects){
    let totalTime = countProjects * 3;
    console.log(`The architect ${architectName} will need ${totalTime} hours to complete ${countProjects} project/s.`);
}

projectsCreation("Gearge",4);