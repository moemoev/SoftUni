function greaterNumber(numberOne, numberTwo){
    if (numberOne > numberTwo){
        console.log(`${numberOne}`);
    }
        else{
            console.log(`${numberTwo}`);   
        }
    
}
greaterNumber(6, 5);

// In this example the {}-brackets could have been skipped since only one line follows the conditionals 
// but better explicit than implicit