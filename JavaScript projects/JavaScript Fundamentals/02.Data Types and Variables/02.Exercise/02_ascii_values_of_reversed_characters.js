function reverseValues(firstChar, secondChar, thirdChar){
    let charReversed = thirdChar + secondChar + firstChar;
    console.log(charReversed);

    //let valuesOfCharReversed = thirdChar.charCodeAt(0)
    console.log(`${thirdChar.charCodeAt(0)} ${secondChar.charCodeAt(0)} ${firstChar.charCodeAt(0)}`);
}